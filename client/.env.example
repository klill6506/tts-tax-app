# Dev auto-login credentials (used by Electron in dev mode)
# Copy this file to .env.local and adjust if needed.
# Create the dev user: poetry run python manage.py create_dev_user
VITE_DEV_USER=dev
VITE_DEV_PASS=dev
